{
  "buildCommand": "pnpm -w run build:packages && pnpm --filter=ai-chatbot run build",
  "installCommand": "pnpm install --filter=ai-chatbot --filter='@turbo-super/*' --recursive",
  "framework": null,
  "env": {
    "TURBO_DISABLE": "1" 
  }
}